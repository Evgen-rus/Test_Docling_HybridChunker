# 🚀 Test Docling HybridChunker

## 📋 Описание проекта

Этот проект представляет собой **полноценную RAG (Retrieval-Augmented Generation) систему** для работы с документами. Система извлекает текст из документов, разбивает его на семантические фрагменты, создает векторные эмбеддинги и предоставляет интерфейс для интеллектуального поиска и чата с документами.

## 🏗️ Архитектура системы

```
Documents → Extraction → Chunking → Embeddings → Search/Chat
    ↓           ↓           ↓          ↓          ↓
   PDF/MD   → Docling  → HybridChunker → OpenAI → LanceDB → Streamlit
```

## 📁 Структура проекта

### 🔧 Основные скрипты (Pipeline)

#### `1-extraction.py` - Извлечение данных
**Технологии:** 
- `docling` - для извлечения текста из PDF/HTML/Markdown
- `DocumentConverter` - универсальный конвертер документов

**Функции:**
- Извлекает текст из PDF, HTML, Markdown документов
- Конвертирует в Markdown формат
- Экспортирует в JSON структуру
- Поддерживает обработку URL (через sitemap)
- Сохраняет результат в `extracted_content.md`

**Поддерживаемые форматы:**
- PDF файлы
- HTML страницы (с возможностью парсинга sitemap)
- Markdown документы

---

#### `2-chunking.py` - Гибридное разбиение
**Технологии:**
- `docling.chunking.HybridChunker` - умное разбиение текста
- Семантический анализ + ограничения по размеру

**Функции:**
- Использует HybridChunker с настройками:
  - `chunk_size=8000` символов
  - `overlap=100` символов перекрытия
- Анализирует семантическую структуру документа
- Учитывает заголовки и структурные элементы
- Сохраняет результат в `chunks_analysis.md`

**Особенности:**
- Балансирует между размером и смыслом
- Может разрезать логические блоки для соблюдения лимитов
- Подходит для технических документов без четкой структуры

---

#### `3-2-simple-chunking.py` - Простое разбиение по этапам
**Технологии:**
- `re` (регулярные выражения)
- Кастомная логика разбиения

**Функции:**
- Разбивает документ строго по заголовкам `## Этап N`
- Создает 21 чанк = 21 этап (точное соответствие)
- Сохраняет полную структуру каждого этапа
- Размеры: 400-2,400 символов на этап
- Сохраняет результат в `simple_chunks_by_stages.md`

**Преимущества:**
- Логичное разбиение по смысловым блокам
- Сохранение связей между этапами
- Предсказуемая структура чанков

---

#### `3-embedding.py` - Создание векторных эмбеддингов
**Технологии:**
- `OpenAI API` - модель `text-embedding-3-small`
- `LanceDB` - векторная база данных
- `Pydantic` - валидация схемы данных

**Функции:**
- Создает векторные представления для каждого чанка
- Сохраняет метаданные (filename, title)
- Использует схему данных `ChunkMetadata` и `Chunks`
- Автоматически индексирует данные в LanceDB
- Поддерживает быстрый семантический поиск

**Схема данных:**
```python
class ChunkMetadata(LanceModel):
    filename: str
    title: str

class Chunks(LanceModel):
    text: str = func.SourceField()
    vector: Vector(1536) = func.VectorField()
    metadata: ChunkMetadata
```

---

#### `4-search.py` - Тестирование поиска
**Технологии:**
- `LanceDB` для векторного поиска
- `OpenAI embeddings` для запросов

**Функции:**
- Тестирует качество поиска по базе знаний
- Выполняет тестовые запросы по логике продаж
- Показывает релевантность результатов (distance score)
- Анализирует статистику базы данных

**Тестовые запросы:**
- "Как проводить квалификацию клиентов?"
- "Что такое тестовый период?"
- "Как оценивать качество лидов?"
- И другие...

---

#### `5-chat.py` - Streamlit чат-интерфейс
**Технологии:**
- `Streamlit` - веб-интерфейс
- `OpenAI GPT-4.1-mini` - языковая модель
- `LanceDB` - поиск контекста
- Streaming API для реального времени

**Функции:**
- Веб-интерфейс для чата с документом
- Автоматический поиск релевантного контекста
- Стриминг ответов в реальном времени
- Показ источников информации
- Примеры готовых вопросов в сайдбаре

**Компоненты:**
- `get_context()` - поиск релевантных фрагментов
- `get_chat_response()` - генерация ответов с контекстом
- Кэширование подключения к БД
- Красивое отображение источников

### 🛠️ Утилиты

#### `utils/sitemap.py` - Парсинг sitemap
**Технологии:**
- `xml.etree.ElementTree` - парсинг XML
- `requests` - HTTP запросы

**Функции:**
- Извлекает URL из sitemap.xml
- Поддерживает разные XML namespaces
- Обработка ошибок (404, timeout)
- Возвращает список URL для массовой обработки

---

#### `utils/tokenizer.py` - Обертка токенизатора
**Технологии:**
- `tiktoken` - токенизатор OpenAI
- `transformers` - совместимость интерфейсов

**Функции:**
- Делает tiktoken совместимым с HuggingFace интерфейсом
- Класс `OpenAITokenizerWrapper` 
- Поддерживает модель `cl100k_base`
- Используется в HybridChunker для точного подсчета токенов

### 📄 Документы и данные

#### `documents/`
- `A1209_инструкция.pdf` - PDF для тестирования
- `domain_certificate.pdf` - дополнительный PDF

#### `data/lancedb/`
- Векторная база данных LanceDB
- Индексы и метаданные

### 📊 Результаты работы

#### `extracted_content.md` (101KB)
- Извлеченный и очищенный текст из документов
- Markdown формат с сохранением структуры

## 🔬 Технологический стек

### 📚 Основные библиотеки
- **Docling 2.34.0** - извлечение и анализ документов
- **OpenAI 1.82.0** - эмбеддинги и языковая модель
- **LanceDB 0.22.1** - векторная база данных
- **Streamlit 1.45.1** - веб-интерфейс
- **Pydantic 2.11.5** - валидация данных

### 🔧 Вспомогательные
- **tiktoken 0.9.0** - токенизация
- **requests 2.32.3** - HTTP запросы
- **python-dotenv 1.1.0** - переменные окружения


## 🚀 Рекомендации

### Для структурированных документов (как у вас)
**Используйте `3-2-simple-chunking.py`:**
- Сохраняет логику документа
- Поддерживает навигационные связи
- Лучшее качество ответов ИИ

### Для неструктурированного контента
**Используйте `2-chunking.py`:**
- Автоматическое семантическое разбиение
- Оптимальные размеры для токенизации
- Работает с любыми документами

## 🎉 Результат

Система успешно создает **интеллектуальную базу знаний** из ваших документов с возможностью:
- 🔍 Семантического поиска
- 💬 Естественного диалога на русском языке
- 📚 Отображения источников информации
- ⚡ Быстрых ответов в реальном времени